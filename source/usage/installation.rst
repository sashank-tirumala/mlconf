
Installation
------------

.. role:: bash(code)
    :language: bash



**PIP**

If you have pip installed, you can simply install the package by running the following command in your terminal:
    
.. code-block:: bash
    
    pip install mlconf

The package is supported on Python 3.6 and above.